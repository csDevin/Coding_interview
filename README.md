# Coding_interview
hello world!
```cpp
left = 0, right = n -1
while (left <= right)
    mid = (left + right) / 2
    case
        x[mid] < t:	left = mid + 1;
        x[mid] = t:	p = mid;	break;
        x[mid] > t: 	right = mid -1;
return - 1;
```

```cpp
#include <iostream>
#include <cmap>
using namespace std;
void main(){
cout<<"test"<<endl;
}
```
